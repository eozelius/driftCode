<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->

  </head>
  <body>
    <% if Rails.env.development? && current_user %>
      <p style="text-align:center">Logged in as: <%= current_user.email %></p>
    <% end %>

    <% if flash.any? %>
      <% flash.each do |msgtype, msg| %>
        <div class="flash-messages flash-<%= msgtype %> col-lg-12 col-md-12 col-sm-12 col-xs-12">
          <p class="flash"><%= msg %></p>
        </div>
      <% end %>
    <% end %>

    <script type="text/javascript">
      $(function(){
        setTimeout(function(){
          $('.flash-messages').remove();
        }, 8000);
      });
    </script>
      
    <!-- side Navigation -->
    <nav id="main">
      <ul>
        <li>
          <%= link_to root_url do %>
            <i class="fa fa-3x fa-globe"></i>
            <p class="nav-legend">Home</p>
          <% end %>
        </li>
       
        <% if logged_in? %>
          <li>
            <%= link_to user_path(current_user) do %>
              <i class="fa fa-3x fa-home"></i>
              <p class="nav-legend">profile</p>
            <% end %>
          </li>
          <li>
            <%= link_to(logout_path, method: :delete) do %>
              <i class="fa fa-3x fa-sign-out"></i>
              <p class="nav-legend">Log out</p>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to(login_path) do %>
              <i class="fa fa-3x fa-key fa-rotate-180"></i>
              <p class="nav-legend">Log in</p>
            <% end %>
          </li>
        <% end %>
      </ul>
    </nav>

    <div class="application-container">
      <div id="page-wrapper">
        <%= yield %>
      </div>  
    </div>
  </body>
</html>

<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-83676035-1', 'auto');
  ga('send', 'pageview');

</script>