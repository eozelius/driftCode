<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>
	
	<p>
		<%= f.label :body %>
		<%= f.text_area :body %>
	</p>

	<button type="submit" class="normal" style="float:right;">comment</button>
<% end %>